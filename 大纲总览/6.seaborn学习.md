Seaborn 学习
│
├── 1. 基础设置
│   │
│   ├── 1.1 导入库
│   │   ├── import seaborn as sns
│   │   ├── import pandas as pd
│   │   └── import matplotlib.pyplot as plt
│   │
│   ├── 1.2 字体设置（中文）
│   │   └── plt.rcParams["font.sans-serif"] = ["STHeiti"]
│   │
│   ├── 1.3 加载数据
│   │   ├── penguins = pd.read_csv("data/penguins.csv")
│   │   └── penguins.dropna(inplace=True)
│   │
│   └── 1.4 数据检查
│       ├── penguins.info()
│       └── penguins.head()
│
├── 2. 分布类图表
│   │
│   ├── 2.1 直方图（histplot）
│   │   ├── sns.histplot(data, x="species")
│   │   └── histplot + kde=True（直方图 + 核密度）
│   │
│   ├── 2.2 核密度估计图（kdeplot）
│   │   ├── 单变量 KDE：sns.kdeplot(x="bill_length_mm")
│   │   ├── 二维 KDE：sns.kdeplot(x="body_mass_g", y="flipper_length_mm")
│   │   └── fill=True, cbar=True（填充 + 颜色条）
│   │
│   └── 2.3 计数图（countplot）
│       └── sns.countplot(x="island")
│
├── 3. 关系类图表
│   │
│   ├── 3.1 散点图（scatterplot）
│   │   ├── sns.scatterplot(x="body_mass_g", y="flipper_length_mm")
│   │   └── hue="sex"（按类别上色）
│   │
│   ├── 3.2 蜂窝图（hexbin 图）
│   │   └── sns.jointplot(kind="hex")
│   │
│   └── 3.3 二维 KDE（密度关系）
│       └── sns.kdeplot(x, y)
│
├── 4. 统计类图表
│   │
│   ├── 4.1 条形图（barplot）
│   │   ├── estimator="mean"（默认求均值）
│   │   └── errorbar=None（不显示误差）
│   │
│   ├── 4.2 箱线图（boxplot）
│   │   └── sns.boxplot(x="species", y="bill_length_mm")
│   │
│   └── 4.3 小提琴图（violinplot）
│       ├── 结合箱线图 + KDE
│       └── sns.violinplot(x, y)
│
├── 5. 多变量可视化
│   │
│   └── 成对关系图（pairplot）
│       ├── sns.pairplot(data, hue="species")
│       ├── 对角线显示单变量（直方图/KDE）
│       └── 非对角显示变量关系（散点图等）
│
└── 6. 图表总结与概念（文件中的知识点）
    │
    ├── 直方图：查看分布形状与频次
    ├── KDE：平滑后的分布估计
    ├── 计数图：分类变量的数量比较
    ├── 散点图：两个连续变量关系
    ├── hexplot：大样本散点密度可视化
    ├── 二维 KDE：关系密度云图
    ├── 条形图：分组统计（均值/中位数）
    ├── 箱线图：分布、异常值
    ├── 小提琴图：分布 + 密度
    └── pairplot：全变量两两关系图